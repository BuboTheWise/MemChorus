repos:
  - repo: local
    hooks:
      - id: version-sync-check
        name: "Check version sync"
        entry: python3 /home/bubo/.hermes/skills/pre-commit-version-sync/scripts/check_version_sync.py .
        language: system
        stages: [pre-commit]
        always_run: true
