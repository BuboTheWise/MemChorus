repos:
  - repo: local
    hooks:
      - id: version-sync-check
        name: "Check version sync (setup.py / README / __init__.py)"
        entry: python3 scripts/check_version_sync.py
        language: system
        stages: [pre-commit]
        always_run: true
