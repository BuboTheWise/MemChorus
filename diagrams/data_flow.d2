vars: {
  d2-config: { layout-engine: elk }
}

direction: right

agent: "Agent"
orchestrator: "Memory Orchestrator"
backends: "Hermes Default + MemPalace"

agent <-> orchestrator
orchestrator <-> backends
