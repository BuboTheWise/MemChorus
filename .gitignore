# ═══ OS / System Artifacts ═══
.DS_Store
Thumbs.db
desktop.ini
*.suo
*.swp
*.bak

# Virtual environments
.venv/
env/
.env

# Python artifacts (build/run intermediates)
__pycache__/
*.pyc
*.pyo
.*.pyc
.egg-info/
.eggs/
.mypy_cache/

# Build & distribution packages
build/
dist/

# Test runner caches, scratch work (not versioned — QA-reports in Bubo_Wisdom)
.pytest_cache/
.coverage
coverage.xml
htmlcov/

# Legacy test scripts at root (moved to tests/ or removed)
test_memchorus.py
test_graceful_degradation.py
test_imports.py
test_hermes_source.py
verify_mempalace_impl.py

# Test scripts (temporary)
*_test.sh
integration_test_*.py
final_test.sh

# Examples moved to examples/ or outside repo
example_usage.py
example_memchorus_usage.py
example_memchorus_usage_improved.py

# Dev setup scripts (not needed in repo)
setup_dev.sh

# Docs moved outside repo
Level-Set-Summary.md

# QA report artifacts (versioned reports live in Bubo_Wisdom, not here)
QA-Reports/
.worktrees/

# Hermes internal task files (not for public repos)
tests/.hermes/

# Internal dev artifacts (not for public repos)
docs/runtimes/
docs/verify/
test_evolution_reports/

# Build artifacts under src (should never be tracked)
src/.eggs/
src/*.egg-info/
